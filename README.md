# file-parser
clone the project using this command 

```git clone https://github.com/drleavio/file-parser.git```

after that just go inside project folder and run this command

```npm install```

at last run this
```npm run dev```


you can use your own database uri of mongodb just create a .env file in root and name the variable (MONGO_URI) and paste the database connection link